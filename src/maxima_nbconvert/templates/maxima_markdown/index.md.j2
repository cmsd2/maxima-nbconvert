{%- extends 'markdown/index.md.j2' -%}

{# Wrap Maxima's tex() output in a GitHub-style ```math fenced code
   block.  Maxima emits bare LaTeX (no delimiters); the standard
   markdown template dumps the text unchanged, leaving the math
   unrendered on GitHub.  The fenced ```math block is GitHub's
   documented way to embed multi-line LaTeX — unlike $$...$$, it
   passes \\ through to MathJax untouched, which matters for matrix
   row separators. #}
{% block data_latex %}
```math
{{ output.data['text/latex'] }}
```
{% endblock data_latex %}
